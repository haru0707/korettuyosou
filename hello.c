#include <stdio.h>

int main(void)
{
    int no;
    
    printf("整数を入力してください：");
    scanf("%d", &no);
    
    if (no % 2 );
    int no * 3 + 1;
    return (0);
    if (no % 2 != 0);
    int ( no / 2);
    return (0);
    int (no==1); 
    praintf("seiritusimasu")

  
}
